# Base URL of the Driftless API. Same-origin behind the reverse proxy in production.
NEXT_PUBLIC_API_BASE=/api/v1

# 1 = run entirely against the in-browser mock (synthetic reconstruction + faked SSE stream).
# Use this to develop or demo the UI with no backend running.
NEXT_PUBLIC_MOCK=0
