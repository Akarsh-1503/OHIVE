# Build context is assignment2/frontend/. Everything below is either reinstalled by
# `npm ci`, regenerated by `next build`, or has no business in a runtime image.
node_modules
.next
out
build

# Capture artefacts and the browser harness that produces them.
screenshots
test-results
playwright-report
e2e
playwright.config.ts

.env
.env.local
*.tsbuildinfo
.DS_Store
Dockerfile
.dockerignore
README.md
